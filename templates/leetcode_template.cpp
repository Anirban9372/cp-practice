#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    void solve(int n) {
        
    }
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    if (cin >> n) {
        Solution sol;
        sol.solve(n);
    }

    return 0;
}
