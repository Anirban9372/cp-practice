#include <bits/stdc++.h>
using namespace std;

// Fast I/O
#define FAST_IO                       \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);

// Type aliases
using ll = long long;
using ull = unsigned long long;
using ld = long double;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
using vi = vector<int>;
using vll = vector<ll>;
using vpii = vector<pii>;
using vpll = vector<pll>;

// Constants
const int INF = 1e9 + 7;
const ll LINF = 1e18 + 7;
const int MOD = 1e9 + 7; // or 998244353
const ld PI = acos(-1.0);

// Macros
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
#define sz(x) (int)(x).size()
#define pb push_back
#define eb emplace_back
#define mp make_pair
#define fi first
#define se second

// Debugging (active only in local environment)
#ifndef ONLINE_JUDGE
#define debug(x) cerr << #x << " = " << (x) << '\n';
#else
#define debug(x)
#endif

void solve()
{
    // Write your solution here
}

int main()
{
    FAST_IO;

    int t = 1;
    cin >> t; // Comment out if single testcase
    while (t--)
    {
        solve();
    }

    return 0;
}
