#include <bits/stdc++.h>
using namespace std;
int main()
{
    cout << fixed << setprecision(9); // tells that 9 decimal places . Without fixed only 9 total significant digits.
    double r;
    double pi = 3.141592653;
    cin >> r;

    cout << pi * r * r;

    return 0;
}