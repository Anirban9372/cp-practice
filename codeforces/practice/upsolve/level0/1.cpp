#include <bits/stdc++.h>
using namespace std;

int main()
{
    // Fast I/O (It is a good habit to always include this)
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Print exactly as requested, using \n for speed
    cout << "Hello World!\n";

    return 0;
}